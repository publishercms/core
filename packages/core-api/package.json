{
  "name": "@publisher-cms/core-api",
  "private": true,
  "description": "",
  "scripts": {
    "dev": "deno run --watch --allow-net --allow-env --allow-read --import-map=import_map.json src/index.ts",
    "check": "deno check src/index.ts",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "db:migrate": "deno run --allow-read --allow-env --allow-net drizzle/cli.ts"
  },
  "dependencies": {
  }
}