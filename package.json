{
  "name": "publisher-cms",
  "version": "1.0.0",
  "description": "Schema based CMS",
  "private": true,
  "scripts": {
    "dev": "docker-compose up -d",
    "dev:logs": "docker-compose logs -f",
    "dev:down": "docker-compose down",
    "dev:rebuild": "docker-compose down && docker-compose up --build -d"
  }
}